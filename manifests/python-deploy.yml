apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-app
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
        - name: python-app
          image: prathmesh2102003/nodejs:64831304
          ports:
            - containerPort: 3000
          env:
            - name: ENV
              value: "production"
